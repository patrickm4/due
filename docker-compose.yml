version: '3.8'

services:
  due:
    build:
      context: .
      dockerfile: dockerfile.dev
    ports:
      - "3000:8080"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
